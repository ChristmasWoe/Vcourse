import React from 'react';
import LabeledCheckbox from '../../entities/LabeledCheckbox/LabeledCheckbox';

const MemorisePassCheckbox = () => {
  return <LabeledCheckbox />;
};

export default MemorisePassCheckbox;
